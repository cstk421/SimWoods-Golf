<Window x:Class="SimLock.Admin.ProcessSelectorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Process to Monitor"
        Width="500" Height="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Select a Running Process"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Foreground="{StaticResource TextPrimaryBrush}"
                   Margin="0,0,0,15"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SearchBox"
                     Grid.Column="0"
                     Margin="0,0,10,0"
                     TextChanged="SearchBox_TextChanged"/>
            <Button Grid.Column="1"
                    Content="Refresh"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Click="Refresh_Click"/>
        </Grid>

        <ListBox x:Name="ProcessList"
                 Grid.Row="2"
                 Background="{StaticResource SurfaceBrush}"
                 Foreground="{StaticResource TextPrimaryBrush}"
                 BorderBrush="{StaticResource PrimaryBrush}"
                 SelectionChanged="ProcessList_SelectionChanged"
                 MouseDoubleClick="ProcessList_MouseDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="5">
                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Width="200"/>
                        <TextBlock Text="{Binding Title}" Foreground="{StaticResource TextSecondaryBrush}"
                                   TextTrimming="CharacterEllipsis" MaxWidth="200"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Cancel"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Background="#666666"
                    Width="100"
                    Margin="0,0,10,0"
                    Click="Cancel_Click"/>
            <Button x:Name="SelectButton"
                    Content="Select"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Width="100"
                    IsEnabled="False"
                    Click="Select_Click"/>
        </StackPanel>
    </Grid>
</Window>
